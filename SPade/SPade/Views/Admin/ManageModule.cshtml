@model IEnumerable<SPade.ViewModels.Admin.ManageModuleViewModel>
@{
    ViewBag.Title = "Manage Modules";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@Scripts.Render("~/bundles/tablescript")
@Styles.Render("~/bundles/tablestyle")

<div class="container">
    <div class="col-md-10 col-md-offset-1">
        <fieldset>
            <legend>Manage Modules</legend>

            <div class="form-group ">
                <div>
                    <a href="AddModule" class="btn btn-primary">Add Module</a>
                </div>
            </div>

            <div class="form-group">
                <div class="input-group">
                    <span class="input-group-addon"><span class="glyphicon glyphicon-search"></span></span>
                    <input id="search" name="search" class="form-control" placeholder="Search" type="text" data-column="any" />
                </div>
            </div>

            <div class="form-group ">
                <table class="table table-striped table-hover tablesorter">
                    <thead>
                        <tr>
                            <th>Module Code</th>
                            <th>Module Name</th>
                            <th>Programming Language</th>
                            <th>Created By</th>
                            <th class="sorter-false">Update</th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(Model => item.ModuleCode)</td>
                                <td>@Html.DisplayFor(Model => item.ModuleName)</td>
                                <td>@Html.DisplayFor(Model => item.ProgrammingLanguage)</td>
                                <td>@Html.DisplayFor(Model => item.CreatedBy)</td>
                                <td><a href="/Admin/UpdateModule/@Html.DisplayFor(Model => item.ModuleCode)" class="btn btn-primary">Update</a></td>
                            </tr>

                        }

                    </tbody>
                </table>
            </div>

        </fieldset>
    </div>
</div>